# 37 — Sync CLAUDE.md with Actual Code

## Problem

CLAUDE.md is missing 7 IPC commands that are implemented:
- `update_expense`, `delete_expenses`
- `get_category_stats`, `create_category`, `rename_category`, `delete_category`, `merge_categories`

## Scope

- Add missing commands to CLAUDE.md's "Key commands" list
- Verify all other listed commands still exist
