[package]
name = "accountant-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "4ccountant"
path = "src/main.rs"

[dependencies]
accountant-core = { path = "../core" }
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
